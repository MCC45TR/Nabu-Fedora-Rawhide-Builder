# Optional USB DisplayLink docks: upstream UDL for old DL-1xx/2xx devices.
# Modern devices use the checksum-pinned external EVDI module plus a separately
# installed DisplayLinkManager. Neither module nor daemon is forced on at boot.
CONFIG_DRM_UDL=m
