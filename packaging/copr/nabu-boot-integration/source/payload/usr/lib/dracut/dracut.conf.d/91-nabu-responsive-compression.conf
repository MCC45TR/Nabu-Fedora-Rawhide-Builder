# Keep post-update UKI generation responsive on Nabu. Dracut's default zstd
# command uses every CPU; two workers bound peak CPU and memory pressure while
# retaining the release compression level.
compress="zstd -15 -q -T2"
