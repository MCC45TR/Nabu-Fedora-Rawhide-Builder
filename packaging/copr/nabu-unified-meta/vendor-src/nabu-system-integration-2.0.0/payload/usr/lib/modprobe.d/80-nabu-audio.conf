# The SM8150 machine driver can probe before the WCD934x MFD codec child has
# caused its ASoC driver to load.  Ensure the codec component and its DAIs are
# registered before snd_soc_sm8150 binds the sound card.
softdep snd_soc_sm8150 pre: snd_soc_wcd934x
