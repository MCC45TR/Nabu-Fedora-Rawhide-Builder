[Service]
# Bound the complete per-user cgroup after its session scopes are stopped.
# "kill" avoids Fedora's additional abort/coredump timeout on shutdown.
TimeoutStopSec=15s
TimeoutStopFailureMode=kill
