[Unit]
# WCN3990's WLAN firmware is requested by the modem over QRTR/TFTP.  rmtfs is
# the first regular service that causes the modem remote processor to boot on
# Nabu, so tqftpserv must already be accepting requests at that point.
Requires=tqftpserv.service
After=tqftpserv.service
