[Service]
# systemd's pre-suspend cgroup freezer can strand Nabu graphical sessions
# before the kernel PM freezer runs. Let the kernel freezer handle processes.
Environment=SYSTEMD_SLEEP_FREEZE_USER_SESSIONS=false
