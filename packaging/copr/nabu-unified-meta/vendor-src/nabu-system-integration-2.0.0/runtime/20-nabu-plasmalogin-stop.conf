[Service]
# The display manager owns helpers outside the desktop's user manager.  Bound
# shutdown even when a broken GPU/DSP session no longer answers logout IPC.
TimeoutStopSec=10s
TimeoutStopFailureMode=kill
