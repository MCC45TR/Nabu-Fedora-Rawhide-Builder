[Service]
# Keep background metadata and package work from competing with the compositor
# on Nabu. These are proportional weights, so PackageKit retains full CPU and
# I/O throughput while the device is otherwise idle.
CPUWeight=20
IOWeight=20
Nice=5
