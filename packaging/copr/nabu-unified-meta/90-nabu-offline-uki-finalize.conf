[Service]
# ExecStartPost belongs to the DNF5 transaction unit itself. Its existing
# Before=reboot.target ordering therefore keeps the machine in the visible
# offline-update boot until every queued Nabu UKI has been generated and the
# selected boot manager has been synchronized.
ExecStartPost=/usr/libexec/nabu-kernel-offline-finalize
