[Service]
# KWin requests the minimum SCHED_RR priority for its compositor, libinput and
# DRM commit threads. Keep the user manager's allowance at that exact minimum;
# KWin drops ambient CAP_SYS_NICE before applying the bounded rlimit.
LimitRTPRIO=1
