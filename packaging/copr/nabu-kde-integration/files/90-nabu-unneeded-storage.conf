# Nabu boots from a plain ext4 partition on its single UFS device.
# Exclude generic desktop-image storage discovery that causes boot errors and
# pulls missing SCSI device-handler requests into the initramfs.
omit_dracutmodules+=" crypt systemd-cryptsetup multipath "
