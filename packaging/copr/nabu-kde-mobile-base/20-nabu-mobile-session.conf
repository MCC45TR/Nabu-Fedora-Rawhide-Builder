[Service]
# Keep the Fedora Plasma Desktop session installed for shared libraries, but
# expose only the mobile session inside Plasma Login Manager's mount namespace.
BindReadOnlyPaths=/usr/share/nabu-plasma-mobile/wayland-sessions:/usr/share/wayland-sessions
