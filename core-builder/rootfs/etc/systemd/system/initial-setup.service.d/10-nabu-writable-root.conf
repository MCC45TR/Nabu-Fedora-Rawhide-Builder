[Unit]
After=systemd-remount-fs.service systemd-logind.service
Requires=systemd-remount-fs.service systemd-logind.service
ConditionPathIsReadWrite=/

[Service]
# Fedora Initial Setup opens a PAM/logind session and writes account state.
# Never start it against a read-only root or before logind is operational.
